#!/usr/bin/env bash

a=10
b=3

sum=$((a + b))
difference=$((a - b))
product=$((a * b))
division=$((a / b))
modulo=$((a % b))

echo "Sum: $sum"
echo "difference: $difference"
echo "Product: $product"
echo "Division: $division"
echo "Modulo: $modulo"
